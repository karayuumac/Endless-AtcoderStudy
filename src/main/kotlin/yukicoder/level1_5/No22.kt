package yukicoder.level1_5

/**
 * @author karayuu
 * Not finished
 */
/*
fun main(args: Array<String>) {
    val (n, k) = readLine()!!.split(" ").map { it.toInt() }
    val s = readLine()!!.toCharArray()

    var before: Pair<Char, Int> = Pair(' ', -1)

    val list = s.filter { it == '(' || it == ')' }

    for (i in list) {
        println("${list.indexOf(i)}  $i")
    }
}
*/