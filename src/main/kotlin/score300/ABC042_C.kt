package score300

/**
 * Created by karayuu on 2018/07/02
 */
/*fun main(args: Array<String>) {
    val (n, k) = readLine()!!.split(" ").map { it.toInt() }
    val dislike = readLine()!!.split(" ").map { it }
    print(check(n, dislike))
}

fun check(checkList: List<String>, dislike: List<String>) : Int {
    val checkList = mutableListOf<String>()

    for (j in checkList) {
        if (dislike.contains(j)) {
            check(checkList + 1, dislike)
        } else {
            break
        }
    }
    return int
}

fun List<Int>.toStringList() : List<String> {
    val result =  mutableListOf<String>()
    this.forEach {
        result.add(it.toString())
    }
    return result
}*/

/*fun Int.isContain(values: List<Int>) {
    if (this.toString().chunked().filter { values.contains(values.toStringList()) == true })
}



fun String.chunked() : List<String> {
    val result = mutableListOf<String>()
    for (i in 0 until this.length) {
        result.add(this[i].toString())
    }
}*/
