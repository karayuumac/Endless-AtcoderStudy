package score300

/**
 * @author karayuu
 */
fun main(args: Array<String>) {
    val (n, a) = readLine()!!.split(" ").map { it.toInt() }
    val x = readLine()!!.split(" ").map { it.toInt() }.toIntArray()
    var count = 0

    /* 深さ優先探索での解法 */
    /* 多分部分点しか入らない(2^16 = 65536通り) */


}
