package ABC084

/**
 * @author karayuu
 */

fun main(args: Array<String>) {
    val (A, B) = readLine()!!.split(" ").map { it.toInt() }

    val post = readLine()!!
    /*
    if (post.get(index = A + 1))
    */
}