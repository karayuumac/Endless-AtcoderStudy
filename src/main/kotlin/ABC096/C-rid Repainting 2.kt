package ABC096

/**
 * @author karayuu
 */
/*
fun main(args: Array<String>) {
    val (r, w) = readLine()!!.split(" ").map { it.toInt() }
    val list = mutableListOf<State>()

    for (i in 1..r) {
        val string = readLine()!!
        for (j in 1..w) {
            list.add(State(i, j, false, string[j - 1].toString()))
        }
    }

    for (state in list) {
        if (state.checked || state.type == ".") {
            continue
        } else {
            state.checked =
        }
    }

    fun canDraw(state: State) : Boolean {
        if ()
    }
}

fun State.canDraw(list: List<State>) : Boolean {
    val up =
}

data class State(val h: Int, val w: Int, val checked: Boolean, val type: String)
        */